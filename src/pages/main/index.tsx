import React, {FC} from "react";
import Header from "../../components/header";

import styles from "./styles.scss";

const Main: FC = () => {
    return (
        <div>
            <Header/>
        </div>
    );
};

export default Main;
